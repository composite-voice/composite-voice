# Proxy /docs/* to the docs site (docs site builds with base: '/docs')
/docs/*  https://composite-voice-docs.netlify.app/docs/:splat  200

# Proxy /design/* to the design system site (design site builds with base: '/design')
/design/*  https://composite-voice-design.netlify.app/design/:splat  200
