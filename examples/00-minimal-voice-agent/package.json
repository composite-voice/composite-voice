{
  "name": "@lukeocodes/composite-voice-example-00-minimal-voice-agent",
  "version": "0.1.0",
  "private": true,
  "description": "Minimal voice agent — NativeSTT (Web Speech API) + Anthropic Claude + NativeTTS (SpeechSynthesis)",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "@lukeocodes/composite-voice": "workspace:*"
  },
  "devDependencies": {
    "vite": "^5.0.0"
  },
  "peerDependencies": {
    "@anthropic-ai/sdk": ">=0.67.0"
  }
}
