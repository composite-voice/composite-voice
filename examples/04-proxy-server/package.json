{
  "name": "@lukeocodes/composite-voice-example-04-proxy-server",
  "version": "0.1.0",
  "private": true,
  "description": "Deepgram + Anthropic + Deepgram via server-side proxy — no API keys in the browser",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "@lukeocodes/composite-voice": "workspace:*"
  },
  "devDependencies": {
    "@anthropic-ai/sdk": ">=0.67.0",
    "@deepgram/sdk": ">=4.11.2",
    "vite": "^5.0.0"
  }
}
