{
  "name": "@lukeocodes/composite-voice-example-10-proxy-server",
  "version": "0.1.0",
  "private": true,
  "description": "Server-side proxy — Deepgram + Anthropic via Express, no API keys in the browser",
  "type": "module",
  "scripts": {
    "dev": "concurrently -n proxy,vite -c blue,green \"PORT=3011 node --env-file=.env --import tsx/esm server.ts\" \"vite\"",
    "dev:proxy": "PORT=3011 node --env-file=.env --import tsx/esm server.ts",
    "dev:vite": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "start": "tsx server.ts"
  },
  "dependencies": {
    "@lukeocodes/composite-voice": "workspace:*"
  },
  "devDependencies": {
    "@anthropic-ai/sdk": ">=0.67.0",
    "concurrently": "^9.0.0",
    "express": "^4.21.0",
    "tsx": "^4.0.0",
    "vite": "^5.0.0",
    "ws": "^8.18.0"
  }
}
