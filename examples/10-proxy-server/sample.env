# API keys for server-side proxy injection.
# These are loaded by vite.config.js and injected into proxy headers.
# They are NOT exposed to the browser bundle.
DEEPGRAM_API_KEY=your_deepgram_api_key
ANTHROPIC_API_KEY=your_anthropic_api_key
