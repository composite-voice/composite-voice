{
  "name": "@lukeocodes/composite-voice-example-50-webllm-pipeline",
  "version": "0.1.0",
  "private": true,
  "description": "Fully offline voice agent — NativeSTT + WebLLM (in-browser via WebGPU) + NativeTTS",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "@lukeocodes/composite-voice": "workspace:*",
    "@mlc-ai/web-llm": "^0.2.74"
  },
  "devDependencies": {
    "vite": "^5.0.0"
  }
}
