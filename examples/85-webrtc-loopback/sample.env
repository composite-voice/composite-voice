# Deepgram API key — used for both STT (Nova) and TTS (Aura) via the Vite proxy
DEEPGRAM_API_KEY=your-deepgram-api-key-here
# Anthropic API key — Claude Haiku generates the agent's replies via the Vite proxy
ANTHROPIC_API_KEY=your-anthropic-api-key-here
