/**
 * @packageDocumentation
 * Provider fallback chain for the `stt` pipeline role.
 *
 * @see {@link FallbackSTT} for the chain implementation
 */

export { FallbackSTT } from './FallbackSTT';
export type { FallbackSTTOptions } from './FallbackSTT';
